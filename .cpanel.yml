# After deploy, use cPanel Terminal (Advanced → Terminal) for artisan/.env fixes—equivalent to SSH.
deployment:
  tasks:
    - export APP_PATH=/home/$USER/baakh_app
    - export PUBLIC_PATH=/home/$USER/public_html
    - /bin/bash scripts/cpanel-post-deploy.sh
